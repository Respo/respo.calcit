# Release respo 0.16.69

- Make the SSR `innerHTML` lookup an explicit Option and unwrap it only after `option:some?`.
- Completes the DOM type-boundary migration needed by older dependents such as respo-value.
- Revalidated native preprocessing, JavaScript generation, and the Vite build.
