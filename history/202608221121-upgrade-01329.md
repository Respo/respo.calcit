# Calcit 0.13.29 升级与 CLI 迁移

- 将项目和 CI 的 Calcit 工具链统一到 0.13.29，使用 `setup-calcit@v1`、`calcit` 和 `caps`。
- 更新 `@calcit/procs` 与 Yarn lock，并保留当前 canonical `calcit.cirru` 快照。
- 将文档与 Markdown 校验脚本中的旧 `cr` 命令统一为 `calcit`。
- 本地验证：`caps --ci`、`calcit calcit.cirru --check-only`、25 个定义测试、111 个 Markdown 示例和 JS codegen 均通过；Vite build 需 Node 24，当前本机 Node 20 不兼容 Vite 8。
