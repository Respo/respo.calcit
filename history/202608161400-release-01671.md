# Release respo 0.16.71

- Guard `comp-global-keydown`'s keyboard handler with `js-present?` and read key/ctrl/meta via `js-nullish->option`, eliminating the `W_JS_FFI_NULLABLE_PREDICATE` warning that blocked dependent projects (e.g. docs-workflow).
- Revalidated native preprocessing, JavaScript generation, and the Vite build.
