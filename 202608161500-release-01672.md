# Release respo 0.16.72

- Revert the global-keydown `effect-listen-keyboard` to the 0.16.70 structure (the 0.16.71 outer-`let` FFI variant broke inlining into dependent projects).
- Guard the keyboard handler with `js-present?` and read key/ctrl/meta via `js-nullish->option`, eliminating the `W_JS_FFI_NULLABLE_PREDICATE` warning for dependents (e.g. docs-workflow) without changing the effect structure.
- Revalidated native preprocessing, JavaScript generation, and the Vite build.
