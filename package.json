{
  "version": "0.16.8",
  "dependencies": {
    "@calcit/procs": "^0.8.43"
  },
  "scripts": {
    "test": "cr --once --emit-js --init-fn=respo.test.main/main! && node test.mjs"
  },
  "devDependencies": {
    "bottom-tip": "^0.1.5",
    "vite": "^5.2.2"
  }
}
