{
  "version": "0.14.42",
  "dependencies": {
    "@calcit/procs": "^0.6.8"
  },
  "scripts": {
    "test": "cr --once --emit-js --init-fn=respo.test.main/main! && node test.mjs"
  },
  "devDependencies": {
    "vite": "^3.1.8"
  }
}
