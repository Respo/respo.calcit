{
  "version": "0.16.11",
  "dependencies": {
    "@calcit/procs": "0.9.5"
  },
  "scripts": {
    "test": "cr --once --emit-js --init-fn=respo.test.main/main! && node test.mjs"
  },
  "devDependencies": {
    "bottom-tip": "^0.1.5",
    "vite": "^5.4.2"
  }
}
