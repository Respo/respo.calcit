{
  "version": "0.14.40",
  "dependencies": {
    "@calcit/procs": "^0.5.46"
  },
  "scripts": {
    "test": "cr --once --emit-js --init-fn=respo.test.main/main! && node test.mjs"
  },
  "devDependencies": {
    "vite": "^2.9.12"
  }
}
