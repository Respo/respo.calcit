{
  "version": "0.14.33",
  "dependencies": {
    "@calcit/procs": "^0.4.31"
  },
  "scripts": {
    "bundle-test": "esbuild test.js --platform=node --bundle --outfile=js-out/bundle.js",
    "test": "cr --once --emit-js --init-fn=respo.test.main/main! && yarn bundle-test && node js-out/bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.12.26",
    "vite": "^2.5.6"
  }
}
