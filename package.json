{
  "version": "0.14.39",
  "dependencies": {
    "@calcit/procs": "^0.5.40"
  },
  "scripts": {
    "test": "cr --once --emit-js --init-fn=respo.test.main/main! && node test.mjs"
  },
  "devDependencies": {
    "vite": "^2.9.6"
  }
}
