{
  "dependencies": {
    "@calcit/procs": "0.10.5"
  },
  "scripts": {
    "test": "cr --once --emit-js --init-fn=respo.test.main/main! && node test.mjs"
  },
  "devDependencies": {
    "bottom-tip": "^0.1.5",
    "vite": "^7.3.1"
  }
}
