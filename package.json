{
  "version": "0.16.16",
  "dependencies": {
    "@calcit/procs": "0.9.20"
  },
  "scripts": {
    "test": "cr --once --emit-js --init-fn=respo.test.main/main! && node test.mjs"
  },
  "devDependencies": {
    "bottom-tip": "^0.1.5",
    "vite": "^7.1.9"
  }
}
