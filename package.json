{
  "version": "0.16.2",
  "dependencies": {
    "@calcit/procs": "^0.8.9"
  },
  "scripts": {
    "test": "cr --once --emit-js --init-fn=respo.test.main/main! && node test.mjs"
  },
  "devDependencies": {
    "vite": "^4.5.0"
  }
}
